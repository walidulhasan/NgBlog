import { ColorgreenDirective } from './colorgreen.directive';

describe('ColorgreenDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorgreenDirective();
    expect(directive).toBeTruthy();
  });
});
